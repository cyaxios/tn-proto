# Carol — the revoked-recipient persona.
#
# Used in tests that need to assert "after revoke, this DID can no
# longer decrypt new envelopes." Carol's role is intentionally the
# "expected to be locked out" one.

role: recipient-revoked
device_seed_hex: ca501ca501ca501ca501ca501ca501ca501ca501ca501ca501ca501ca501ca50
passphrase: carol-regression-passphrase
notes: |
  Revoked recipient. Starts as an active reader, gets revoked mid-
  ceremony, then attempts to decrypt — assertion is that her kit no
  longer works on subsequent envelopes.
